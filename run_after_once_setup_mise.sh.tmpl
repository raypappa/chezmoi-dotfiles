{{ if eq .chezmoi.os "darwin" "linux" -}}
#!/bin/sh
export PATH=~/.local/bin:$PATH
if type mise 2>&1 >/dev/null;then
mise install gh
mise x gh auth login
export GITHUB_TOKEN=$(mise x gh auth token)
mise install
fi
{{ end -}}
